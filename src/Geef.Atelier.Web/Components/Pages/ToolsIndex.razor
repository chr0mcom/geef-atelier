@page "/tools"
@attribute [Authorize]
@rendermode InteractiveServer
@using Geef.Atelier.Core.Domain.Tools
@using Geef.Atelier.Web.Services
@inject IToolDefinitionService ToolService
@inject NavigationManager Nav

<PageTitle>Tools — Atelier</PageTitle>

<div class="crew-profiles-index">
    <div class="page-header">
        <h1>Tools</h1>
        <a href="/tools/create" class="btn primary">+ New tool</a>
    </div>

    @if (_tools.Count == 0)
    {
        <p class="text-muted">No tools configured yet.</p>
    }
    else
    {
        <table class="crew-table" data-testid="tool-list">
            <thead>
                <tr>
                    <th>Name</th>
                    <th>Type</th>
                    <th>Access</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var tool in _tools)
                {
                    <tr data-testid="tool-row-@tool.Name">
                        <td>
                            @tool.DisplayName
                            @if (tool.IsSystem)
                            {
                                <span class="crew-badge">System</span>
                            }
                            @if (tool.AccessClass == ToolAccessClass.Mutating)
                            {
                                <span class="crew-badge crew-badge--danger">Mutating</span>
                            }
                        </td>
                        <td>@GetTypeLabel(tool.ToolType)</td>
                        <td>@GetAccessLabel(tool.AccessClass)</td>
                        <td class="crew-table-actions">
                            <a href="/tools/view/@tool.Name" class="btn small">View</a>
                            @if (tool.IsSystem)
                            {
                                <button class="btn small primary" @onclick="() => DuplicateTool(tool)">Duplicate as custom</button>
                            }
                            else
                            {
                                <a href="/tools/edit/@tool.Name" class="btn small">Edit</a>
                                <button class="btn small danger" @onclick="() => _toolToDelete = tool">Delete</button>
                            }
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }

    @if (!string.IsNullOrEmpty(_error))
    {
        <div class="error-banner">@_error</div>
    }
</div>

<DeleteConfirmationModal
    Show="_toolToDelete != null"
    ItemName="@(_toolToDelete?.Name ?? "")"
    ItemKind="Tool"
    OnConfirm="ExecuteDeleteAsync"
    OnCancel="() => _toolToDelete = null" />

@code {
    private IReadOnlyList<ToolDefinition> _tools = [];
    private ToolDefinition? _toolToDelete;
    private string? _error;

    protected override async Task OnInitializedAsync()
    {
        await LoadAsync();
    }

    private async Task LoadAsync()
    {
        _tools = await ToolService.GetAllAsync();
    }

    private async Task DuplicateTool(ToolDefinition original)
    {
        _error = null;
        try
        {
            var copyName = "custom-" + original.Name.TrimStart("custom-".ToCharArray()) + "-copy";
            var copy = original with
            {
                Name = copyName,
                DisplayName = original.DisplayName + " (copy)",
                IsSystem = false
            };
            await ToolService.SaveAsync(copy);
            await LoadAsync();
        }
        catch (Exception ex)
        {
            _error = ex.Message;
        }
    }

    private async Task ExecuteDeleteAsync()
    {
        if (_toolToDelete == null) return;
        var name = _toolToDelete.Name;
        _toolToDelete = null;
        try
        {
            await ToolService.DeleteAsync(name);
            await LoadAsync();
        }
        catch (Exception ex)
        {
            _error = ex.Message;
        }
    }

    private static string GetTypeLabel(string toolType) => toolType switch
    {
        "web-search"        => "Web search",
        "knowledge-base"    => "Knowledge base",
        "url-fetch"         => "URL fetch",
        "news-search"       => "News search",
        "academic-search"   => "Academic search",
        "rest-api"          => "REST API",
        "static-context"    => "Static context",
        "learning-retrieval" => "Learning retrieval",
        "mcp-tool"          => "MCP tool",
        _                   => toolType
    };

    private static string GetAccessLabel(ToolAccessClass access) => access switch
    {
        ToolAccessClass.ReadOnly  => "Read-only",
        ToolAccessClass.Mutating  => "Mutating",
        _                         => access.ToString()
    };
}

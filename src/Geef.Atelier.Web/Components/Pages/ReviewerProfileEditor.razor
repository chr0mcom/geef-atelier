@page "/crew/profiles/reviewers/new"
@page "/crew/profiles/reviewers/{name}"
@attribute [Authorize]
@rendermode InteractiveServer
@using Geef.Atelier.Application.Crew
@using Geef.Atelier.Core.Domain.Crew
@using Geef.Atelier.Core.Domain.Crew.Profiles
@inject ICrewService CrewService
@inject NavigationManager Nav

<PageTitle>@(IsNew ? "Reviewer-Profil erstellen" : "Reviewer-Profil bearbeiten") — Atelier</PageTitle>

<div class="crew-editor" data-testid="reviewer-profile-editor">
    <div class="page-header">
        <h1>@(IsNew ? "Neues Reviewer-Profil" : _isSystem ? _displayName : $"Bearbeiten: {_displayName}")</h1>
        <a href="/crew/profiles/reviewers" class="btn ghost">← Zurück</a>
    </div>

    @if (_loading)
    {
        <p>Wird geladen…</p>
    }
    else
    {
        <ProfileEditorForm ProfileName="@(IsNew ? null : Name)"
                           DisplayName="@_profileDisplayName"
                           Description="@_profileDescription"
                           SystemPrompt="@_profileSystemPrompt"
                           Provider="@_profileProvider"
                           Model="@_profileModel"
                           MaxTokens="@_profile?.MaxTokens"
                           IsSystem="@_isSystem"
                           IsSaving="@_saving"
                           Error="@_error"
                           OnSave="SaveAsync"
                           OnCancel="NavigateBack"
                           OnDuplicate="DuplicateAsCustom"
                           OnDeleteRequested="() => _showDeleteModal = true" />
    }
</div>

<DeleteConfirmationModal
    Show="_showDeleteModal"
    ItemName="@(Name ?? "")"
    ItemKind="Profile"
    OnConfirm="ExecuteDeleteAsync"
    OnCancel="() => _showDeleteModal = false" />

@code {
    [Parameter] public string? Name { get; set; }

    private bool IsNew => Name == null;
    private bool _isSystem;
    private bool _loading = true;
    private bool _saving;
    private bool _showDeleteModal;
    private string? _error;
    private string _displayName = "";
    private ReviewerProfile? _profile;

    // Computed properties to avoid null-coalescing in Razor component attributes
    private string _profileDisplayName => _profile?.DisplayName ?? "";
    private string _profileDescription => _profile?.Description ?? "";
    private string _profileSystemPrompt => _profile?.SystemPrompt ?? "";
    private string _profileProvider => _profile?.Provider ?? "";
    private string _profileModel => _profile?.Model ?? "";

    protected override async Task OnInitializedAsync()
    {
        if (!IsNew)
        {
            _profile = await CrewService.GetReviewerProfileAsync(Name!);
            if (_profile == null) { Nav.NavigateTo("/crew/profiles/reviewers"); return; }
            _isSystem = _profile.IsSystem;
            _displayName = _profile.DisplayName;
        }

        _loading = false;
    }

    private void NavigateBack() => Nav.NavigateTo("/crew/profiles/reviewers");

    private async Task SaveAsync(ProfileEditorForm.ProfileFormValues values)
    {
        _saving = true;
        _error = null;
        try
        {
            var profile = new ReviewerProfile(
                Name: SystemCrew.EnsureCustomPrefix(values.Name),
                DisplayName: values.DisplayName,
                Description: values.Description,
                SystemPrompt: values.SystemPrompt,
                Provider: values.Provider,
                Model: values.Model,
                MaxTokens: values.MaxTokens,
                IsSystem: false);

            if (IsNew)
            {
                await CrewService.CreateCustomReviewerProfileAsync(profile);
            }
            else
            {
                if (profile.Name != Name)
                    await CrewService.RenameCustomReviewerProfileAsync(Name!, profile.Name);
                await CrewService.UpdateCustomReviewerProfileAsync(profile);
            }

            Nav.NavigateTo("/crew/profiles/reviewers");
        }
        catch (Exception ex)
        {
            _error = ex.Message;
        }
        finally
        {
            _saving = false;
        }
    }

    private async Task DuplicateAsCustom()
    {
        _error = null;
        try
        {
            var original = await CrewService.GetReviewerProfileAsync(Name!);
            if (original == null) return;
            var copy = original with
            {
                Name = original.Name + "-copy",
                DisplayName = original.DisplayName + " (Copy)",
                IsSystem = false
            };
            await CrewService.CreateCustomReviewerProfileAsync(copy);
            Nav.NavigateTo("/crew/profiles/reviewers");
        }
        catch (Exception ex)
        {
            _error = ex.Message;
        }
    }

    private async Task ExecuteDeleteAsync()
    {
        _showDeleteModal = false;
        _error = null;
        try
        {
            await CrewService.DeleteCustomReviewerProfileAsync(Name!);
            Nav.NavigateTo("/crew/profiles/reviewers");
        }
        catch (Exception ex) { _error = ex.Message; }
    }
}
